# Resume Bullets

Replace every bracketed value with measured output from your completed experiment.

- Streamed and aggregated 220M+ e-commerce clickstream events into a UTC-normalized hourly demand dataset using memory-efficient JSONL processing.
- Built leakage-aware rolling train, validation, and test windows with training-only preprocessing for 24-hour cart and order forecasting.
- Developed a compact PyTorch Transformer and benchmarked it against persistence, seasonal-naive, and Ridge models using MAE, RMSE, WAPE, and horizon-level analysis.
- Reduced average test MAE by **[X%]** versus the strongest baseline and packaged the selected model with reproducible artifacts, automated tests, Docker, and a FastAPI inference service.

Do not claim an improvement when the Transformer is not the strongest model. Replace “Transformer” with the actual winning model and explain the comparison honestly.
